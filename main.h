#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _printf(const char *format, ...);
char _putchar(char c);
int print_string(char *str);
int print_int(int n);
#endif
