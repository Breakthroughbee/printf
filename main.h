#ifndef MAIN_H
#define MAIN_H
#include <stdio.h>
#include <stdarg.h>
#include <unistd.h>

int _printf(const char *format, ...);
int _putchar(int c);
int print_string(char *str);
int  print_int(int n);
int printchar(char c);
#endif
